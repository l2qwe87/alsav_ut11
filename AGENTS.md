# AGENTS.md

## Информация о проекте

### Язык разработки
- Разработка ведётся на платформе 1С:Предприятие

### Правила работы с Git
- **ВАЖНО**: Не создавать коммиты в main
- **ВАЖНО**: Не мёржить PR
- Описания коммитов и PR делать на русском языке
- Создавать PR утилитой `gh pr` с флагом `--body-file file`
- Описание PR предварительно сохранять в файл
- После создания PR удалять временный файл
- **ВАЖНО**: Временные файлы описания PR не коммитить в репозиторий



### Структура проекта
- `ext_alsav/` - основное расширение с бизнес-логикой (регистры накопления, бизнес-процессы, подсистемы CRM, закупок, продаж, склада)
- `ext_b24/` - расширение для интеграции с Битрикс24 (модули HTTP/JSON, сопоставление клиентов)
- `base/` - базовая конфигурация (исключена из gitignore)

### Конвенции кода
- Следовать существующему стилю кода
- Использовать существующие библиотеки и утилиты
- Следовать паттернам проекта
- **Обработка исключений**: Не использовать блоки `Попытка-Исключение` для перехвата ошибок. Позволять платформе обрабатывать исключения стандартными средствами. Стандартные формы ошибок 1С:Предприятие являются приемлемыми для пользователей.

### Работа с метаданными 1С
- **ОБЯЗАТЕЛЬНО**: Перед изменением любого объекта метаданных использовать MCP сервер `1c-metadata` для анализа связей
- **ОБЯЗАТЕЛЬНО**: После изменения XML файлов метаданных проверять синтаксис с помощью инструмента `1c-syntax-checker`
- Использовать `search_metadata` для поиска зависимостей перед модификацией
- Использовать `get_metadata_structure` для понимания структуры конфигурации
- Использовать `find_object_by_name` для поиска связанных объектов

### Работа с API Битрикс24
- **ОБЯЗАТЕЛЬНО**: Перед использованием методов API Битрикс24 проверять их существование через `check_bitrix24_method`
- Использовать `list_bitrix24_modules` для ознакомления с доступными модулями API
- Использовать `get_bitrix24_method_info` для получения подробной информации о параметрах метода
- Использовать `search_bitrix24_methods` для поиска подходящих методов по функциональности
- **ПРИМЕЧАНИЕ**: MCP сервер `bitrix24-api-checker` работает офлайн и содержит актуальную базу методов API Битрикс24

### Проверка синтаксиса и качества кода
- **ОБЯЗАТЕЛЬНО**: Проверять синтаксис всех измененных XML файлов с помощью `1c-syntax-checker`
- **ОБЯЗАТЕЛЬНО**: Выполнять проверку синтаксиса при генерации кода 1С (файлов *.bsl) через `1c-syntax-checker`
- **ПРИМЕЧАНИЕ**: При проблемах с MCP сервером `1c-syntax-checker`, можно вызывать проверку синтаксиса напрямую через Bash:
  ```bash
  node .opencode\mcp\1c-syntax-checker\bsl-syntax-checker.js "путь/к/файлу.bsl"
  ```
- Использовать MCP сервер `1c-help` для проверки корректности конструкций языка 1С
- Использовать `get_1c_syntax` для проверки синтаксиса конструкций (Если, Для, Пока и т.д.)
- Использовать `get_1c_examples` для получения примеров правильного кода
- Использовать `search_1c_help` для поиска информации о функциях и процедурах

### Порядок работы с изменениями
1. **Анализ**: Использовать `search_metadata` для поиска связанных объектов
2. **Изменение**: Вносить изменения в код с учетом существующих паттернов
3. **Проверка синтаксиса**: Использовать `1c-syntax-checker` для всех XML и BSL файлов
4. **Проверка кода**: Использовать `1c-help` MCP сервер для проверки конструкций 1С
5. **Проверка API Битрикс24**: При работе с интеграцией использовать `check_bitrix24_method` для проверки методов
6. **Тестирование**: Убедиться что все изменения корректны

### Доступные инструменты
- **MCP сервер 1c-metadata**:
  - `search_metadata` - поиск по метаданным
  - `get_metadata_structure` - структура конфигурации
  - `find_object_by_name` - поиск объектов по имени
- **Инструмент 1c-syntax-checker** - проверка синтаксиса XML файлов
- **MCP сервер 1c-help**:
  - `search_1c_help` - поиск по справке 1С
  - `get_1c_syntax` - синтаксис конструкций
  - `get_1c_examples` - примеры кода
  - `search_web_help` - онлайн-справка
- **MCP сервер bitrix24-api-checker**:
  - `check_bitrix24_method` - проверка существования методов API Битрикс24
  - `list_bitrix24_modules` - получение списка доступных модулей API
  - `get_bitrix24_method_info` - подробная информация о методе API
  - `search_bitrix24_methods` - поиск методов по ключевому слову

### Правила работы с типами метаданных

#### Справочники (Catalogs)
- Перед изменением реквизитов использовать `search_metadata` для поиска форм и модулей
- Проверять наличие связанных документов через поиск по имени справочника
- После изменения обязательно проверять синтаксис XML файла справочника

#### Документы (Documents)
- Анализировать движения документов по регистрам через `search_metadata`
- Проверять связанные печатные формы и отчеты
- Убедиться что все реквизиты имеют корректные типы данных

#### Регистры (AccumulationRegisters, InformationRegisters)
- Проверять структуру измерений, ресурсов и реквизитов
- Искать связанные отчеты и обработки
- Анализировать использование в документах

#### Общие модули (CommonModules)
- Использовать `get_1c_examples` для проверки правильности кода
- Проверять вызовы процедур и функций через `search_metadata`
- Использовать `get_1c_syntax` для проверки синтаксиса конструкций

#### Обработки и отчеты (DataProcessors, Reports)
- Проверять формы и модули на корректность
- Искать связанные объекты метаданных
- Проверять СКД (систему компоновки данных) в отчетах

### Примеры использования инструментов

#### Перед изменением справочника:
```
/search_metadata query="Номенклатура" metadataType="Catalogs"
/find_object_by_name objectName="Номенклатура"
```

#### После изменения XML или BSL файла:
```
Использовать инструмент 1c-syntax-checker для проверки синтаксиса
```

#### При написании кода 1С:
```
/get_1c_syntax construct="Если"
/get_1c_examples topic="работа с датами"
/search_1c_help query="СтрНайти" category="функция"
```

#### При работе с API Битрикс24:
```
/check_bitrix24_method method="crm.lead.add"
/list_bitrix24_modules
/search_bitrix24_methods query="lead"
/get_bitrix24_method_info method="user.get"
```

### Чек-лист проверки изменений

#### Перед коммитом изменений:
- [ ] Использован `search_metadata` для анализа зависимостей
- [ ] Все измененные XML и BSL файлы проверены через `1c-syntax-checker`
- [ ] Код 1С проверен через `get_1c_syntax` и `get_1c_examples`
- [ ] Проверены связанные объекты через `find_object_by_name`
- [ ] Использованы существующие паттерны проекта
- [ ] При работе с API Битрикс24 использован `check_bitrix24_method` для проверки методов

#### При работе с формами:
- [ ] Проверена корректность XML структуры формы
- [ ] Использованы правильные типы данных для реквизитов
- [ ] Проверены обработчики событий формы

#### При работе с модулями:
- [ ] Использованы правильные конструкции языка 1С
- [ ] Проверен синтаксис процедур и функций через `1c-syntax-checker`
- [ ] Использованы существующие функции проекта

#### При работе с запросами:
- [ ] Проверен синтаксис запроса через `get_1c_examples`
- [ ] Использованы правильные имена таблиц и полей
- [ ] Проверены условия и параметры запросов

### Частые ошибки и как их избежать

#### Ошибки в XML метаданных:
- **Проблема**: Некорректная структура XML
- **Решение**: Всегда проверять синтаксис через `1c-syntax-checker`

#### Ошибки в коде 1С:
- **Проблема**: Неправильный синтаксис конструкций
- **Решение**: Использовать `get_1c_syntax` для проверки и `1c-syntax-checker` для BSL файлов
- **Альтернатива**: При проблемах с MCP сервером вызывать проверку напрямую:
  ```bash
  node .opencode\mcp\1c-syntax-checker\bsl-syntax-checker.js "путь/к/файлу.bsl"
  ```

#### Ошибки в зависимостях:
- **Проблема**: Нарушение связей между объектами
- **Решение**: Использовать `search_metadata` для анализа

#### Ошибки в типах данных:
- **Проблема**: Несоответствие типов
- **Решение**: Проверять типы через `search_1c_help`

#### Ошибки в API Битрикс24:
- **Проблема**: Использование несуществующих методов API
- **Решение**: Всегда проверять методы через `check_bitrix24_method`
- **Проблема**: Неправильные параметры метода
- **Решение**: Использовать `get_bitrix24_method_info` для получения информации о параметрах
- **Проблема**: Поиск подходящего метода
- **Решение**: Использовать `search_bitrix24_methods` для поиска по функциональности

### Безопасность
- Никогда не коммитить секреты и ключи
- Следовать лучшим практикам безопасности

## Документация по MCP серверам

### Bitrix24 API Checker

**Расположение:** `.opencode/mcp/bitrix24-api-checker/`

**Назначение:** Проверка процедур и методов API Битрикс24 на основе официальной документации

**Функциональность:**
- ✅ Проверка существования методов API Битрикс24
- ✅ Получение подробной информации о методах (параметры, ответ)
- ✅ Поиск методов по ключевым словам
- ✅ Просмотр доступных модулей API
- ✅ Интеллектуальные предложения при опечатках
- ✅ Офлайн работа без интернет-соединения

**Поддерживаемые модули:**
- **CRM** - лиды, компании, контакты, сделки
- **User** - управление пользователями
- **Tasks** - задачи и проекты
- **Disk** - файлы и хранилище
- **Calendar** - календарь и события
- **IM** - сообщения и чаты
- И другие модули (landing, catalog, sale, socialnetwork, и т.д.)

**Примеры использования:**

Проверка метода:
```
/check_bitrix24_method method="crm.lead.add"
```

Поиск методов:
```
/search_bitrix24_methods query="lead" module="crm"
```

Информация о методе:
```
/get_bitrix24_method_info method="user.get"
```

Список модулей:
```
/list_bitrix24_modules
```

**CLI интерфейс:**
```bash
cd .opencode/mcp/bitrix24-api-checker
node cli-standalone.js check crm.lead.add
node cli-standalone.js search lead
node cli-standalone.js list
```

**Документация:** Подробная документация в `.opencode/mcp/bitrix24-api-checker/README.md`

### 1C Metadata

**Расположение:** `.opencode/mcp/1c-metadata/`

**Назначение:** Поиск и анализ метаданных конфигурации 1С

**Функциональность:**
- ✅ Поиск объектов метаданных по имени и типу
- ✅ Анализ структуры конфигурации
- ✅ Поиск зависимостей между объектами
- ✅ Работа со справочниками, документами, регистрами

### 1C Syntax Checker

**Расположение:** `.opencode/mcp/1c-syntax-checker/`

**Назначение:** Проверка синтаксиса BSL файлов и XML метаданных

**Функциональность:**
- ✅ Проверка синтаксиса BSL файлов через Language Server
- ✅ Автономный анализатор для базовых проверок
- ✅ Поиск стилистических проблем
- ✅ Проверка канонического написания ключевых слов

### 1C Help

**Расположение:** `.opencode/mcp/1c-help/`

**Назначение:** Доступ к справке по языку 1С и примерам кода

**Функциональность:**
- ✅ Поиск по встроенной справке 1С
- ✅ Получение синтаксиса конструкций языка
- ✅ Примеры правильного кода
- ✅ Доступ к онлайн-документации

## Интеграция MCP серверов

Все MCP серверы автоматически интегрированы в `opencode.json`:

```json
{
  "mcp": {
    "1c-metadata": { "enabled": true },
    "1c-help": { "enabled": true },
    "1c-syntax-checker": { "enabled": true },
    "bitrix24-api-checker": { "enabled": true }
  }
}
```

Для использования любого инструмента достаточно вызвать его через opencode с соответствующими параметрами.