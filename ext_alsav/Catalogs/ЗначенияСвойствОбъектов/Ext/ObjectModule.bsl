
&После("ПередЗаписью")
Процедура алсав_ПередЗаписью(Отказ)
	// Вставить содержимое метода.
	Попытка
		сорт = Число(ЭтотОбъект.ПолноеНаименование);
		ЭтотОбъект.Сортировка = сорт;
	Исключение
	КонецПопытки;
КонецПроцедуры
